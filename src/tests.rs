#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = 2 + 1;
        assert_eq!(result, 4);
    }


    #[test]
    fn it_doesnt_work() {
        let result = 2 + 1;
        assert_eq!(result, 4);
    }
}